<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Lets Chaka install her own updates (downloaded from GitHub Releases). -->
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
  <application>
    <!-- Shares the downloaded APK with the system installer via a content:// URI. -->
    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.updates"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/chaka_update_paths" />
    </provider>
    <service
      android:name="com.chakamyth.hands.ChakaKeepAliveService"
      android:foregroundServiceType="dataSync"
      android:exported="false" />
    <service
      android:name="com.chakamyth.hands.ChakaNotificationService"
      android:label="Chaka Notifications"
      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
      android:exported="false">
      <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
      </intent-filter>
    </service>
    <service
      android:name="com.chakamyth.hands.ChakaAccessibilityService"
      android:label="Chaka Hands"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
      android:exported="true">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/chaka_accessibility_config" />
    </service>
  </application>
</manifest>
